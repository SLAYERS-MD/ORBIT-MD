export default {
  command: [],
  tags: "",
  desc: "",
  group: false,
  admin: false,
  owner: false,

  run: async (sock, m, { args, text, prefix, command, config }) => {
    // lógica del plugin
  }
}

